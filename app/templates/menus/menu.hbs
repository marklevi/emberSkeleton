<div class="menu">
    <div class="location__menu-title">{{model.menu1.displayTitle}}</div>
    {{#each model.menu1.submenus as |submenu|}}
        <div class="location__submenu-title">{{submenu.displayTitle}}</div>
        <ul>
            {{#each submenu.menuItems as |menuItem|}}
                <li>
                  <span class="menu-item">{{menuItem.displayTitle}}</span>
                  <span class="price-item">{{price-resolver menuItem model.menu1priceband}}</span>
                </li>
            {{/each}}
        </ul>
    {{/each}}
</div>
